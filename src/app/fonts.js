import { Abril_Fatface } from 'next/font/google'
import { Alfa_Slab_One } from 'next/font/google'
import { Inter_Tight } from 'next/font/google'
import { Be_Vietnam_Pro } from 'next/font/google'



export const abril_fatface = Abril_Fatface({ subsets: ['Latin'], weight: '400' }) // No se usa

export const alfa_slab_one = Alfa_Slab_One({ subsets: ['Latin'], weight: '400' }) // No se usa

export const inter_tight = Inter_Tight({ subsets: ['Latin'], weight: '400' })

export const be_vietnam_pro = Be_Vietnam_Pro({ subsets: ['Latin'], weight: '400' })